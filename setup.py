from setuptools import setup, find_packages

setup(
    name='mlcernet',
    version='0.1.0',
    packages = find_packages(),
    install_requires=[],
    entry_points={
        'console_scripts': [],
    },
    description='cervical screening tool',
    classifiers=[
        'Programming Language :: Python :: 3',
    ],
)
